module objects {
  export interface ButtonData {
    buttonText: string,
    targetID: number,
    feedbackText: string,
    requirement: string,
    requirementMinAmount: string,
    selectLimit: number
  }
}
