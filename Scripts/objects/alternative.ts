module objects {
  export interface Alternative {
    caption: string,
    targetID: number,
    feedbackText: string,
    requirement: string,
    requirementMinAmount: string,
    selectLimit: number
  }
}
